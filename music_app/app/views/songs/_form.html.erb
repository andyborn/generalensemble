<%= form_for(@song) do |f| %>
  <% if @song.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@song.errors.count, "error") %> prohibited this song from being saved:</h2>

      <ul>
      <% @song.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title, {placeholder: "Required! max 50 characters"} %>
  </div>
  <div class="field">
    <%= f.label :about %><br />
    <%= f.text_area :about,  {placeholder: "Optional! max 500 characters"}%>
  </div>
  <div class="field">
    <%= f.label :song_file %><br />
    <%= f.file_field :song_file %>
  </div>
  <div class="field">
    <%= f.label :image %><br />
    <%= f.file_field :image %>
  </div>
 

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
